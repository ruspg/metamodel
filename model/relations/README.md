# model/relations

> **Note:** This directory is reserved for future use if the relation catalog
> grows beyond ~120 relations and requires domain-based decomposition.
>
> Currently all relation kinds live in `model/relation_catalog.yaml` as a
> single cohesive file. See the
> [design rationale](../../README.md#authoring-structure-design-rationale)
> for why.

Architecture references for relation contracts remain under `docs/architecture/`.
