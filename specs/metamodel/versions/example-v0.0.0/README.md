# Example version directory (shape only)

This directory demonstrates the required artifact layout for a single imported metamodel bundle version.

Replace this placeholder directory with real imported version directories, for example `2026.03.0`.
